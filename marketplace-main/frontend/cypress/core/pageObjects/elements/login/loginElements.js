const titleImg = 'img.auth0-lock-header-logo'
const emailInp = 'input[type=email]'
const initSubmitButton = 'button[type=submit]'
const passwordInp = 'input[type=password]'
const submitButton = 'button[name="submit"]'

const loginElements = {
    titleImg,
    emailInp,
    initSubmitButton,
    passwordInp,
    submitButton
}

module.exports = loginElements